/* const errors = {
    name_lenght_string: "name length must be ate least 5 characters long",
    exists: "Product already exists",
    qts_zero: "quantity must be larger than or equal to 1",
}

const nameCheck = (value) => value.length > 5 && value === String;

const code = 422;

switch (true) {
    case nameCheck(name) return { code, errors.name_lenght_string }
} */